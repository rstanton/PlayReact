
var diagramDDoc = {
  _id:'_design/diagrams',
  views:{
    by_name:{
      map:function(doc) {
       emit(doc.name);
      }.toString()
    }
  }
}

var applicationDDoc = {
  _id:'_design/applications',
  views:{
    by_name:{
      map:function(doc) {
        emit(doc.appName);
      }.toString()
    }
  }
}

var schemaDDoc = {
  _id:'_design/schemas',
  views:{
    by_name:{
      map:function(doc) {
        emit(doc.title);
      }.toString()
    }
  }
}

var createIndex=function(dbconn, index){
    dbconn.put(index, function(err, doc){
      if(err)
        if(err.status!=409)
          console.error(err);
    });
}